# FormatBuffer-String
A C++ string formatting system with type-safe arguments using a specialized buffer. Includes a custom String class and FormatBuffer for safe argument passing. Supports format specifiers: %i (int), %d (double), %c (char), %s (string).
